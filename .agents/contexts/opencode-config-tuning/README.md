# OpenCode Config Tuning

## State

- Status: draft plans, no config changes approved yet
- Last updated: 2026-06-22
- Next step: choose which track to execute first: OpenCode/OMO config fixes or token-overhead profile design. Any OpenCode config edit requires an explicit per-file approval and same-directory `.bak` backup.

## Purpose

Track durable planning notes for tuning local OpenCode and Oh-My-OpenAgent behavior without losing them in ignored `.omo/` runtime state. This context covers keybindings/model/category fixes, tmux integration verification, and token-overhead profile design.

## Files

- `oh-my-openagent-config-fixes.md` - draft plan for Ctrl+Enter submit behavior, `unspecified-low` model/category selection, and tmux integration verification.
- `token-overhead-profiles.md` - draft plan for lean/dev/full OpenCode profiles to reduce startup/context token overhead.

## Load policy

Read this file first. Read `oh-my-openagent-config-fixes.md` before changing OpenCode/OMO settings, keybinds, category models, or tmux MCP integration. Read `token-overhead-profiles.md` before designing profile wrappers or slimming OpenCode token overhead.

## Legacy inputs

Ported from ignored local drafts:

- `.omo/drafts/oh-my-openagent-config-fixes.md`
- `.omo/drafts/opencode-token-overhead-profiles.md`
