# OpenCode Config Tuning

## State

- Status: archived/closed; OpenCode/OMO config-fix track live-verified and complete; token-overhead profile idea rejected
- Last updated: 2026-06-27
- Next step: none. No active implementation work remains in this archived context. Keep it as historical evidence for later analysis unless a new, narrower OpenCode/OMO tuning request appears. Any future OpenCode config edit still requires an explicit per-file approval and same-directory `.bak` backup.

## Purpose

Track durable planning notes for tuning local OpenCode and Oh-My-OpenAgent behavior without losing them in ignored `.omo/` runtime state. This context covers keybindings/model/category fixes, tmux integration verification, and token-overhead profile design.

## Files

- `oh-my-openagent-config-fixes.md` - current status for Ctrl+Enter submit behavior, `unspecified-low` model/category selection, OMO runtime verification, and tmux integration findings.
- `token-overhead-profiles.md` - rejected lean/dev/full profile idea plus retained diagnostics about MCP/package token-overhead risks.

## Load policy

Read this file first. Read `oh-my-openagent-config-fixes.md` before changing OpenCode/OMO settings, keybinds, category models, or tmux integration. Read `token-overhead-profiles.md` only as historical context; do not resume the rejected profile-wrapper plan unless the user gives a new, narrower request.

## Legacy inputs

Ported from ignored local drafts:

- `.omo/drafts/oh-my-openagent-config-fixes.md`
- `.omo/drafts/opencode-token-overhead-profiles.md`
