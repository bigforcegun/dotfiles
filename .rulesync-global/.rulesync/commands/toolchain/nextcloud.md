---
targets:
  - '*'
description: Nextcloud files via the toolchain proxy
---
Scope `/toolchain` to Nextcloud: `retrieve_tools` with `query: "nextcloud file <English action from $ARGUMENTS>"`.

List the directory to confirm a path before reading or writing it. Never overwrite or delete without showing me the target first.
