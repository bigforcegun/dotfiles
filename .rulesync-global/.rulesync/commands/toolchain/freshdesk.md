---
targets:
  - '*'
description: Support tickets via the toolchain proxy
---
Scope `/toolchain` to Freshdesk: `retrieve_tools` with `query: "freshdesk ticket <English action from $ARGUMENTS>"`.

Ticket bodies are customer data — sensitivity `private`. Never reply or change status without my confirmation.
