---
targets:
  - '*'
description: Slack via the toolchain proxy
---
Scope `/toolchain` to Slack: `retrieve_tools` with `query: "slack <English action from $ARGUMENTS>"`.

Resolve the channel or user id before posting. Posting is outward-facing: show me the exact text and target first.
