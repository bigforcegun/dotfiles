---
targets:
  - '*'
description: My live browser session via the toolchain proxy — acts as me
---
Scope `/toolchain` to my own browser: `retrieve_tools` with `query: "browser page <English action from $ARGUMENTS>"`, then take the hit whose `server` is named after my browser or profile (`*-personal`). The clean server offers the same names at the same score — choosing by rank lands in the wrong browser silently.

This session holds my real logins, so every action is mine. Read freely with `browser_snapshot` and `browser_find`. Navigate, click or type only after showing me the target. Never submit, post, purchase or authorise on my behalf.
