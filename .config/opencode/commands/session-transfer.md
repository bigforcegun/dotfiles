---
description: Find and rehome an OpenCode session through oc-session.
---

Use `oc-session` only for this request: $ARGUMENTS

List projects or sessions first when a project or session is ambiguous. Use `oc-session rehome` for a transfer. It relocates the session to the target project with the same ID; it does not create a clone. Do not call `opencode export`, `opencode import`, or the database directly.
